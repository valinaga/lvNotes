<li class="arrow">
  <%= letter.message ? letter.message.content : ''%> 
  <%= link_to("Resend me another 3 choices", resend_url, :class => "short") if letter.pending?%><br />
  <span class="small"><%= l letter.sent, :format => :short %></span>
</li>
<!--
      <tr class="row-<%= letter.status.downcase%>">
        <td><%= l letter.sent, :format => :short %></td>
        <td><%= letter.status %></td>
        <td><%= letter.message ? letter.message.content : ''%> <%= link_to("Resend me another 3 choices", resend_url) if letter.pending?%></td>
      </tr>
-->
